#!/bin/bash
#
# Mirrors postinst: PSCode never wrote an apt source or keyring, so there is none to remove.

rm -f /usr/bin/@@NAME@@

update-alternatives --remove editor /usr/bin/@@NAME@@ 2>/dev/null || true

# Uninstall the desktop entry
if hash update-desktop-database 2>/dev/null; then
	update-desktop-database
fi

# Update mimetype database for removed workspace mimetype
if hash update-mime-database 2>/dev/null; then
	update-mime-database /usr/share/mime
fi
